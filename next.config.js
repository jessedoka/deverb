/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            '66.media.tumblr.com',
        ]
    }
}

module.exports = nextConfig
